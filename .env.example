# Copy this to .env and fill in real values. Never commit .env.

# --- GitHub App credentials ---
# Created at https://github.com/settings/apps -> New GitHub App
GITHUB_APP_ID=
GITHUB_APP_WEBHOOK_SECRET=
# Path to the private key (.pem) downloaded from the GitHub App settings.
GITHUB_APP_PRIVATE_KEY_PATH=./private-key.pem

# --- LLM provider ---
# Groq is the default (fast Llama inference). Get a key at https://console.groq.com
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# --- Review behavior ---
# Optional: extra instructions appended to the reviewer system prompt.
# Examples:
#   REVIEW_STYLE_NOTES="Focus only on security issues; ignore style nitpicks."
#   REVIEW_STYLE_NOTES="Be especially blunt about untested code paths."
REVIEW_STYLE_NOTES=

# --- Server ---
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# --- Database ---
# SQLite for local dev, Postgres in prod.
DATABASE_URL=sqlite+aiosqlite:///./prsage.db
